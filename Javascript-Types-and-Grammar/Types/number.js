/**
 * The number in javascrript
 * 
 * Jacascript provided number literal and number object
 * let deeping it. 
 * its base that beginner must know.
 * to create number we can using one of these methods
 * 
 * number Literal
 * number Object (Also call primitives).
 */

/**
 * Javacscript number literal.
 */
let number = 0;

/**
 * Checking the type in javascript.
 */
typeof number; // return "number".
typeof number === "number"; // will return true.